#include <stdio.h>
#include <stdlib.h>

struct node {
    int data;
    struct node *next;
};

int size(struct node *head) {
    if(!head) return 0;
    int c = 0;
    struct node *p = head;
    do {
        c++;
        p = p->next;
    } while(p != head);
    return c;
}
